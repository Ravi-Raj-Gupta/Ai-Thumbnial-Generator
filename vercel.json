{
   "version": 2,
   "buildCommand": "npm run build",
   "outputDirectory": "dist",
   "builds": [
      {
         "src": "server/server.ts",
         "use": "@vercel/node"
      }
   ],
   "routes": [
      {
         "src": "/(.*)",
         "dest": "server/server.ts"
      }
   ]
}
